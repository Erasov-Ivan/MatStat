**Статистика** - произвольно измеримая функция, зависящая от элементов выборки
**Несмещённая оценка** - статистика, мат ожидание от которой равно параметру 
**Асимптотически несмещённая оценка** - статистика, мат ожидание о которой стремится к параметру
**Состоятельная оценка** - статистика, значение которой стремится по вероятности к параметру
**Асимптотически нормальная оценка параметра** - $\sqrt{n} * (тета - параметр)^2$ стремится к нормальному распределению $N(0, \sqrt{D})$. 
Эквивалентная запись $(тета - параметр)^2 / (\sqrt{n} * \sqrt{D})$ -> N(0, 1)

**Теорема о достаточных условиях состоятельности оценки** - асимптотически нормальная оценка является состоятельной

**Свойства выборочного среднего**
- Если мат ожидание случайной величины конечно то выборочное среднее является несмещённой оценкой мат ожидания 
- Если мат ожидание от модуля случайно величины кончено то выборочное среднее является состоятельной оценкой мат ожидания 
- Если дисперсия случайной величины конечна и != 0 то выборочное среднее является асимптотически нормальной оценкой мат ожидания

**Начальный выборочный момент k-го порядка** - $\bar{x^k} = \sum_{i=1}^{n}x_i^k$
**Свойства начального выборочного момента k-го порядка**
- g - случайная величина
- $\bar{x^k}$ - несмещённая оценка $Mg^k$
- $\bar{x^k}$ - состоятельная оценка $Mg^k$
- Если $Dx^k$ - конечно и != 0, то $\bar{x^k}$ - асимптотически нормальная оценка $Mg^k$

**Выборочная дисперсия** $S^2 = 1/n * \sum_{i=1}^n{x_i}$
**Исправленная выборочная дисперсия** $\bar{S^2} =  1/(n - 1) * \sum_{i=1}^n{x_i}$
**Свойства** $S^2$ и $\bar{S^2}$
- Пусть дисперсия случайной величины конечна и != 0, тогда
- $S^2$ - смещённая оценка дисперсии
- $\bar{S^2}$ - несмещённая оценка дисперсии
- $S^2$ - асимптотически несмещённая оценка дисперсии
- $S^2$ и $\bar{S^2}$ - состоятельные оценки дисперсии
- $S^2$ и $\bar{S^2}$ - асимптотически нормальные оценки дисперсии

**Свойства эмпирической функции распределения**
- $Fg^*(y)$ - эмпирическая, $Fg(y)$ - теоретическая функции распределения 
- $Fg^*(y)$ - несмещённая оценка $Fg(y)$ - несмещённая оценка $Fg(y)$
- Если $Fg(y)$ != 0 и != 1 то $D(Fg^*(y)) = Fg(y) * (1 - Fg(y)) / n$
- Если $Fg(y)$ != 0 и != 1 то $Fg^*(y)$ - асимптотически нормальная оценка $Fg(y)$
- Случайная величина $n*Fg^*(y)$ имеет биномиальное распределение B(n, F(y))

**Запоминаем и не путаем буквы**
- θ - тета ($f_{θ}$ - тут тета выглядит как 0, но это не 0, это тета, будьте внимательны!)
- Θ - тета большое

**Метод моментов**
- Пусть задана выборка $x_1$ . . . $x_n$ из известного распределения, зависящего от параметра θ из множества Θ, лежащего в R
- Выбираем функцию g: R -> R, такую что:
	- Существует $M(g(x_i)) = h(θ)$, причём h - обратима на Θ
- θ = $h^{-1} * M(g(x_i))$ 
- θ* = $h^{-1} * \bar{g(x)}$ - оценка параметра θ
- А если без воды, то при одном параметре приравниваем мат ожидание к выборочному среднему и считаем уравнение, при двух параметров добавляем что дисперсия равна исправленной выборочной дисперсии и считаем систему уравнений
**Теорема о свойствах оценок, полученных методом моментов** - если функция g - непрерывна, то θ* - состоятельная оценка параметра тета


**Метод максимального правдоподобия**
- Пусть задана выборка $x_1$ . . . $x_n$ из известного распределения, зависящего от параметра θ из множества Θ
-  $f_{θ}$ - плотность если величина абсолютно непрерывная, вероятность если дискретная 
- Функция правдоподобия = $f_{θ}(x_1)$ * . . . * $f_{θ}(x_n)$
- Логарифмическая функция правдоподобия = логарифм от функции правдоподобия (предыдущий пункт) 
- Оценкой максимального правдоподобия параметра тета называется максимум функции правдоподобия на тета большое.
- Кратко: надо найти где максимум функции правдоподобия, для этого берём логарифмическую функцию правдоподобия, считаем производную, приравниваем к нулю, получаем точки локальных максимумов / минимумов. Считаем вторую производную в этих точках, там где вторая производная отрицательна - там и точка максимума. Если таких точек нет - то это максимум на какой то из границ области определения.

**Эффективность оценок**
- По базе задана выборка $x_1$ . . . $x_n$ из известного распределения, зависящего от параметра тета из множества тета большое (параметр тета одномерный)
- Оценка $θ_1^*$ лучше $θ_2^*$ **в среднеквадратичном смысле** если $M(θ_1^* - θ)^2 <= M(θ_2^* - θ)^2$  и хотя бы на одном θ неравенство строгое (<)
- **Теорема о самой лучшей оценке** - среди всевозможных оценок наилучше в среднеквадратичном смысле не существует
- **Классы смещённых оценок** - класс $K_b$ это такое оценки $θ^*$ что $Mθ^* = θ + b(θ)$
- Оценка θ* - **эффективная оценка** в классе $K_b$ если она является лучше в среднеквадратичном смысле среди других оценок в этом классе
- Эффективная оценка в классе $K_0$ - **просто эффективная оценка**
- **Замечание** - в классе несмещённых оценок для поиска эффективной достаточно сравнить дисперсии т.к. эффективной является несмещённой оценкой 
**Теорема о 2 эффективных оценках в одном классе** - если существует 2 эффективные оценки в одном классе, то они равны (в конспекте над знаком равно буквы "п. и." что бы это не значило)

**Неравенство Рао-Крамера** 
- **Носителем параметрического семейства распределений Fθ** называется **любое множество C ⊆ R**, такое, что при всех θ ∈ Θ выполняется равенство **P(X1 ∈ C) = 1**
- **Информация Фишера** $I(θ) = M(d/{d_θ}*(\ln{f_θ(x_1)}))^2$
- **Условия регулярности**:
	- Существует носитель С такой что для любого θ и для любого y функция $\sqrt{f_θ(y)}$ - непрерывно дифференцируема по θ на множестве Θ
	- Информация Фишера существует, положительна и непрерывна по θ на Θ
- **Неравенство Рао-Крамера для смещённых оценок**
	- Пусть семейство распределений $f_θ$ удовлетворяет условиям регулярности
	- Тогда для любой оценки θ* из $K_b$ дисперсия которой ограничена на любых константах из Θ выполняется неравенство 
	- $M(θ - θ*)^2 >= (1 + b'(θ))^2/(n*I(θ)) + (b(θ))^2$
- **Для несмещённых оценок**
	- $D(θ) >= 1/(n*I(θ)$)
- **Леммы**
	- Пусть семейство распределений $f_θ$ удовлетворяет условиям регулярности 
	- Тогда для любой статистики T, дисперсия которой ограничена на любых константах из Θ выполняется
	- Лемма 1: $(M(T))'_θ = M(T * L'_θ(x_1 . . . x_n, θ))$
	- Лемма 2: $|M(gη) - M(g)M(η)| <= \sqrt{DgDη}$ g и η - случайные величины

**Доверительные интервалы**
- **α-квантиль** - число $Z_α$ такое что $f_g(Z_α) <= α$ : $\lim_{x->α}f_g(x)=α$
- $f_g(x)$ - функция распределения 
- Интервал (θ1; θ2) называется **доверительным** для параметра θ с уровнем значимости α если вероятность попадания θ в этот интервал = 1 - α
- $θ_0 = (θ_1 + θ_2) / 2$ - середина доверительного интервала
- $e = (θ_2 - θ_1) / 2$ - точность доверительного интервала
- $(θ_1; θ_2) = (θ_0 - e; θ + e)$
- 1 - α  - доверительная вероятность (уровень значимости)
- α - вероятность ошибки 
- Формулы для расчётов доверительных интервалов для параметров нормального и биномиального распределений найдёте в табличке, сюда я их писать не буду

**Обработка результатов экспериментов по методу наименьших квадратов**
- Известно что случайная величина y из R зависит линейно от случайной величины x из R 
- y = $\sum_{i=1}^k{a_i*x^i}$ где $a_i$ неизвестны 
- Провели n экспериментов и получили результаты 

| $x_1^1$ | . . . | $x_1^k$ | $y_1$ |
| ------- | ----- | ------- | ----- |
| $x_2^1$ | . . . | $x_2^k$ | $y_2$ |
| . . .   | . . . | . . .   | . . . |
| $x_n^1$ | . . . | $x_n^k$ | $y_n$ |
- Надо восстановить a = $(a_1, a_2 . . . a_k)$
- $y_m = \sum_{i=1}^k{a_i * x_m^i}$
- Там дальше ничего не понятно, как мне кажется надо просто расписать в общем виде метод наименьших квадратов 
- **Основные зависимости, решаемые методом наименьших квадратов** - по сути просто табличка замен чтобы привести уравнение к многочлену (табличку прикреплю) 


**Критерии проверки гипотез**
- Пусть заданы выборка из неизвестного распределения F 
- **Гипотезой H** называется любое предположение о распределении F
- H = {F = F1} - просто гипотеза
- H = {F принадлежит какому то классу распределений} - сложная гипотеза 
- Есть всегда 2 гипотезы: H0 - основная и H1 - альтернативная 
- **Тестовой статистикой T** называется статистика, значением которой определяет выбор той или иной гипотезы
- Вероятность принять H1 когда верна H0 = α - ошибка 1 рода
- Вероятность принять H0 когда верно H1 = β - ошибка 2 рода
- **Минимаксный критерий** - есть 2 критерия, у них есть вероятности ошибок $α_1, β_1, α_2, β_2$ и критерий 1 лучше критерия 2 в смысле минимаксного подхода если $max(α_1, β_1) <= max(α_2, β_2)$
- **Байесовский критерий** - также 2 критерия, но ещё есть вероятность что верна гипотеза H0 = r, и вероятность что верна гипотеза H1 = s (r + s = 1). Также известна линейная функция потерь. Потери от неверного решения равны $r'$ если произошла ошибка 1 рода и $s'$ если произошла ошибка 2 рода. $r = r' / (r' + s')$. $s = s'/(r' + s')$. Тогда вот критерий 1 лучше критерия 2 в смысле байесовского подхода если $r * α_1 + s * β_1 <= r * α_2 + s * β_2$
- Критерий называется **байесовским** если он лучше других в смысле байесовского подхода
- **Наиболее мощным критерием** - в классе $K_e$ (среди всех критериев у которых α <= e) называется критерий, у которого β <= чем у остальных
- **Критерии согласия и их суть**
	- В качестве тестовой статистики выбираем некоторую меру D отклонения эмпирической функции распределения от теоретической 
	- Ищем $D_0$ так чтобы  $p(D <= D_0) <= 1 - α$ (α - уровень значимости)
	- И если наша мера отклонения $D < D_0$ то принимаем H0, иначе H0 отвергаем
 